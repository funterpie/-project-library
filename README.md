# -project-library
Library Management System built in Python with Streamlit UI. Users can borrow, return, and view books. Admins can add/remove books and manage users. Data is stored in JSON for persistence. Features include user login, admin authentication, and real-time book availability tracking
